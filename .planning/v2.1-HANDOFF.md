# v2.1-hardening — Session Handoff

**Updated:** 2026-08-12 21:10 CDT
**Branch:** `v2.1-hardening` (from main @ c8c3686)
**Master plan:** `.planning/v2.1-PLAN.md` (copy of `/home/cstory/.claude/plans/fan-out-review-the-generic-heron.md`)
**Process:** superpowers:subagent-driven-development — one implementer per task, independent review gate per task, fix loops, deferred-minors ledger, final whole-branch review at the end.
**SDD workspace (gitignored, on-disk):** `.superpowers/sdd/fan-out-review-the-generic-heron/` — contains `progress.md` (authoritative ledger), `task-N-brief.md` (requirements per task), `task-N-report.md` (implementer evidence), `review-*.diff` packages.

## Task status (16 tasks)

| # | Task | Status | Commits |
|---|------|--------|---------|
| T1 | godot.ts foundations (parseOperationOutput/runOperation/resolveWithinProject/ensureProject) | ✅ complete, reviewed | 5dd3b6a, b9fe497 |
| T2 | godot_operations.gd fail() contract, exit-code dispatcher fix, debug-noise removal | ✅ complete, reviewed | 16a063b |
| T3 | scene.ts migration (7 sites) + tests | ✅ complete, reviewed | add0a80 |
| T4 | uid/resource/script migration + create_resource/modify_resource .gd contract + param-mismatch fixes + validate/list_scripts summary-recovery fix | ✅ complete, reviewed | f8b659c, 18cb077 |
| T5 | composition/config/animation/shader/tilemap migration (26 sites, 5 test files) + param audit | ⚠️ IN PROGRESS — implementer died; **uncommitted partial edit to src/tools/composition.ts only**; brief at task-5-brief.md is complete and battle-tested | — |
| T6 | project/scaffold/testing/export migration | 📋 brief ready (task-6-brief.md) | — |
| T7 | path hardening rollout + godot-resources.ts arbitrary-read fix | pending (brief not yet written; see plan Track A Step 3) | — |
| T8 | resize_image.gd injection fix | pending (plan Track A Step 4) | — |
| T9 | small correctness fixes | pending (plan Track A Step 5) + ledgered addition: parseOperationOutput tier-3 must treat stderr `SCRIPT ERROR:` as failure (observed live: GDScript runtime error → exit 0, no JSON → silent false success) | — |
| T10 | Track B: npm scripts/CI/tsconfig tests/registry smoke/13 untested tools/integration suite/start.sh | pending | — |
| T11 | Track C pt 1: withProject(), output unification, logger | pending | — |
| T12 | Track C pt 2: headless awareness, batched collision, caps, dedupe, overwrite flag, autoloads | pending | — |
| T13 | Track D: docs + planning hygiene | pending | — |
| T14 | Track E1: zero-setup runtime helpers | pending | — |
| T15 | Track E2: send_input/invoke_runtime/wait_for | pending | — |
| T16 | Track E3: structured errors + incremental debug output | pending | — |

Baseline tests: 472 → now 534+ passing (`npx vitest run`); `npx tsc --noEmit` clean at every commit. Godot 4.7.1 on PATH; live smoke scratch project: `/tmp/claude-1000/-home-cstory-src-godot-mcp/56ceb247-ad1e-4ea3-b87d-142f0e6c0d58/scratchpad/gd-smoke/project` (recreate trivially if tmp cleared: minimal project.godot).

## Hard-won knowledge (do not lose)

1. **TS↔GDScript param-name mismatches are a silent bug class** (camelCase vs snake_case; unit tests can't catch — mocks hide it). T4 found 5/7 tools broken. T5/T6 briefs REQUIRE a per-op audit of TS params vs `.gd` `params.get()` reads + live smoke per module. Check `convertCamelToSnakeCase` in godot.ts before concluding mismatch.
2. **Ops without a success envelope** (validate_scripts, list_scripts, query_class): plain `!result.ok` gate destroys them when engine stderr has `Failed to `/error markers. Use script.ts's `findSummaryJson` recover-before-error pattern.
3. **`SCRIPT ERROR:` stderr marker not in tier-3** → unhandled GDScript runtime errors are still silent false successes. Scheduled for T9.
4. **Deferred minors + parked findings** live in the ledger (`progress.md`) — the final whole-branch review must triage them. Notables: query_class stderr exposure; resave_resources never fails; dead executeOperation mock scaffolding in resource/script test files; scene.ts tests assert mock args not response shapes (6/7); `{}` params treated as parse failure in .gd (falsy Dictionary).

## Known operational issue: subagent stalls (verified)

3 of 5 implementer dispatches died before delivering (impl-t4 3.5h silent; impl-t4b wrote report, never committed, claimed it had; impl-t5 died mid-task after 1 file). Reviewer final messages also frequently fail to auto-deliver.

Mitigations in force (keep using):
- **30-min detached watchdog** per dispatch (`sleep 1800 && echo WATCHDOG...` in background Bash); on expiry: check commits/report file mtimes, ping via SendMessage, kill + redispatch if silent. (User-mandated; also in persistent memory `subagent-watchdog-timers`.)
- Instruct every agent: **report via SendMessage to "main"**; commit + message are the LAST actions; no lingering.
- When an agent dies with finished work in the tree: controller verifies independently (vitest + tsc + live smoke) and commits itself (user authorized this pattern for this effort).
- User directive: **all subagents on model=fable** for this effort.

## Resuming in a new session

1. `git checkout v2.1-hardening` in /home/cstory/src/godot-mcp.
2. Read `.superpowers/sdd/fan-out-review-the-generic-heron/progress.md` (ledger = source of truth; tasks with a `complete` line are DONE — never re-dispatch).
3. T5 first: decide whether to keep the uncommitted composition.ts partial (check `git diff src/tools/composition.ts` against task-5-brief.md) or reset it; then dispatch a fresh implementer on task-5-brief.md. Consider splitting T5 into two slices (composition+config+animation / shader+tilemap) to fit inside watchdog windows.
4. Then T6 (brief ready) → T7…T16 per the plan's track order. Write briefs for T7+ from `.planning/v2.1-PLAN.md` sections, carrying the ledger's NOTE lines into the relevant briefs.
5. After T16: final whole-branch review (most capable model) over `git merge-base main HEAD`..HEAD + ledger triage, one fix wave, then superpowers:finishing-a-development-branch.
